/* @flow */
import React from 'react';
import ButtonIcon from '../ButtonIcon/ButtonIcon';

const SettingsIcon = (props: Object) => {
    return ( <ButtonIcon icon="settings" size="sm" {...props} /> );
};

export default SettingsIcon;